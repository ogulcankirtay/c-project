#include <stdio.h>
#include <stdlib.h>

int main()
{
   /* int s=0;
char kelime[100];
printf("bir cumle giriniz: ");
fgets(kelime,100,stdin);
int i;
for(i=0;kelime[i]!='\0';i++){
}
printf("kelimenþn uzunlugu %d",i);
printf("aramak istediginiz harfi girin: ");
char a;
scanf("%c",&a);
for(i=0;kelime[i]!='\0';i++){
    if(kelime[i]==a)
        s=1;
}
if(s==1)
    printf("var");
else
    printf("yok");
*/
char cumle1[100];
char cumle2[100];

}
